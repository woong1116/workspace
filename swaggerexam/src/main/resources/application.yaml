spring:
  application:
    name: swaggerexam


  datasource:
    url: jdbc:mysql://localhost:3306/caramidb
    username: carami
    password: carami1234

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        format_sql: true

